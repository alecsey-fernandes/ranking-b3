fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
apscheduler==3.10.4
pytest==8.3.3

# sqlite3 é da biblioteca padrão do Python — nenhuma dependência extra
# necessária para a persistência histórica no MVP.

# Descomentar ao migrar para Postgres em produção (ver README > "Migrando para Postgres"):
# sqlalchemy==2.0.35
# psycopg2-binary==2.9.9
# python-dotenv==1.0.1
